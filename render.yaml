# LEGACY CLUSTER REFERENCE ONLY - DO NOT DEPLOY.
# Keep the existing service and disk identifiers unchanged for audit history.
# Canonical V2 specifications are render.v2.yaml and render.v2.persistent.yaml.
# The x-legacy key intentionally prevents Render from finding deployable services.
x-legacy-cluster-services:
  - type: web
    name: glamping-cluster-app
    runtime: node
    plan: starter
    region: singapore
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    disk:
      name: glamping-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
      - key: DATA_DIR
        value: /var/data
      - key: OUTPUTS_DIR
        value: /var/data/outputs
      - key: CONFIG_DIR
        value: /var/data/config
      - key: APP_PIN
        sync: false
      - key: NAVER_CLIENT_ID
        sync: false
      - key: NAVER_CLIENT_SECRET
        sync: false
      - key: NAVER_SEARCHAD_API_KEY
        sync: false
      - key: NAVER_SEARCHAD_SECRET_KEY
        sync: false
      - key: NAVER_SEARCHAD_CUSTOMER_ID
        sync: false
