# 사분 데이터랩 Master DB V1

## 결론

자료 형식은 엑셀이나 CSV로 통일하지 않습니다. 엑셀·CSV·JSON은 수집 원본과 내보내기 형식으로 남기고, 앱이 사용하는 공통 원장은 `Master DB` 하나로 통일합니다.

1차 구현은 기존 서비스를 바꾸지 않는 **Shadow DB**입니다.

- 운영 예정 경로: `/var/data/master_db/sabun_master.sqlite`
- 실행 기준: Node.js 22.13 이상
- 기존 `outputs/`, `company_master/`, `history/`, `tourism_data/`는 삭제하거나 수정하지 않습니다.
- 파일은 SHA-256, 크기, 상대경로와 함께 증거자료로 색인합니다.
- 로그인 비밀번호, 회원 개인정보, API 키는 분석 Master DB에 넣지 않습니다.
- 업체 관리자 자료의 사업자번호·연락처·이메일·비밀값은 DB의 분석용 JSON에서 마스킹합니다.
- 기존 화면과 API는 아직 기존 파일을 읽습니다. 동일성 검증 후 단계적으로 전환합니다.

Shadow 단계의 원본 증거는 기존 파일에 그대로 보존되고 Master DB에는 실제 내용을 다시 계산한 SHA-256과 위치를 기록합니다. 운영 원장 전환 전에는 Render 영구 디스크 백업과 증거파일 보존정책까지 함께 검증합니다.

## 하나의 DB 안에서 분리하는 영역

### 1. 고정 마스터

- 공식 행정구역과 상하위 관계
- 기존 지역키·표시명·별칭
- 관광공사 시군구 코드
- 업체, 업체 별칭, Place ID, 예약사업자 ID
- 숙박 업종과 수집 키워드

업체명+주소, 업체명+지역은 자동 병합키로 사용하지 않고 `company_match_candidates`에 검토 후보로 저장합니다.

### 2. 수집 실행과 증거

- 어떤 키워드와 기간으로 수집했는지
- 회차 상태와 관측기간
- 회차 안의 대상별 작업과 재시도 결과
- 결과파일의 상대경로, 크기, SHA-256
- 같은 파일과 각 관측행을 다시 반입했는지 확인하는 ledger

### 3. 관측값

- 업체별 예약일, 순위, 공급량, 잔여량, 판매추정, 가격
- 업체별 채널 확인설정과 상품 단위 수량·가격 관측
- 네이버 검색광고 키워드 검색량·클릭·CTR
- 행정구역별 방문자, 체류·소비강도, 관광자원 수요, 관광다양성
- 관광 데이터랩 12개월 누적값은 월별로 임의 분해하지 않고 원래 기간을 보존

관측행은 수정하지 않고 회차별로 계속 추가합니다. 화면이 사용할 값은 `*_current` 포인터가 정상도와 수집시각을 기준으로 선택합니다. 따라서 나중에 오류가 발생해도 먼저 저장된 정상값은 유지되며, 오류 회차 자체도 감사이력으로 남습니다.

### 4. 관리자 확정·보정·검토

- 관리자 확정 기본정보
- 객실·수량 기준 보정
- 채널 설정과 검토결정
- 내부 실제 예약자료

자동관측 원본과 관리자 보정은 물리적으로 구분합니다. 보정을 해제해도 원관측을 복구할 필요 없이 조합 규칙만 바뀝니다.

## 갱신 주기

| 자료 | Master DB 갱신시점 | 외부 호출 |
|---|---|---|
| 행정구역·관광 지역코드 | 공식 기준 변경 때 | 필요할 때만 |
| 지역 관광 API 12개월 | 최신 확정월 기준 월 1회 | 정기수집·관리자 실행만 |
| 네이버 Place·키워드·트렌드 | 업체/업종 수집 회차 | 수집 기능에서만 |
| 업체 내부 실제자료 | 관리자가 반입할 때 | 없음 |
| 화면 조회 | 저장된 Master DB 읽기 | 없음 |

## 상태와 결측 원칙

- `complete`: 정상 관측
- `partial`: 일부만 관측
- `no_data`: 정상 응답이지만 해당 자료 없음
- `not_supported`: 원천에서 제공하지 않음
- `error`: 통신·파싱·차단 오류
- `stale`: 사용기한이 지난 자료
- `rejected`: 검증 탈락

원천에서 실제 숫자 `0`을 반환한 때만 0으로 저장합니다. 자료 없음·차단·오류는 `NULL`이며, 이후 오류값은 기존 `complete` 값을 덮지 못합니다.

## 안전한 반입 명령

기본 명령은 검사만 하고 DB나 원본을 변경하지 않습니다.

```powershell
npm run master-db:dry-run
```

검사 결과를 확인한 다음에만 Shadow DB를 생성합니다.

```powershell
npm run master-db:apply
```

`--apply`는 초기 이관을 위한 오프라인 일회성 작업입니다. 웹 서버 시작 명령에 넣거나 운영 서버가 수집자료를 쓰는 동안 실행하지 않습니다. 운영 갱신은 다음 단계에서 수집 회차 하나만 짧게 기록하는 증분 저장으로 연결합니다.

운영 반입 전에는 `/var/data` 복제본으로 DB와 최대 WAL 용량을 측정하고 그 합계의 2배 이상 여유공간을 확인합니다. 백업은 WAL checkpoint 뒤 DB를 복사하거나 `.sqlite`, `-wal`, `-shm`을 함께 보존합니다. Shadow-read 동일성 검증과 복귀 스위치가 끝나기 전에는 Master DB를 단독 원본으로 사용하지 않습니다.

다른 데이터 폴더를 검사할 때는 다음처럼 지정합니다.

```powershell
node scripts/master_db_import.cjs --dry-run --data-dir C:\data
node scripts/master_db_import.cjs --apply --data-dir C:\data --db C:\data\master_db\sabun_master.sqlite
```

## 전환 순서

1. 현재 파일의 건수와 SHA-256을 기록합니다.
2. Shadow DB에 반입합니다.
3. 같은 반입을 두 번 실행해도 행 수가 늘지 않는지 확인합니다.
4. 월명글램핑 업체 상세, 대표 수집회차, 산청군 12개월 지표를 기존 API와 대조합니다.
5. 기존 응답 모양을 그대로 만드는 호환 조회 계층을 추가합니다.
6. 화면에는 기존값을 제공하면서 차이만 기록하는 Shadow-read를 시행합니다.
7. 지역 조회 → 업체 검색·상세 → 수집 쓰기 순으로 전환합니다.
8. 안정화 기간에는 기존 파일 읽기 방식으로 즉시 복귀할 수 있게 유지합니다.

## 현재 1차 검증 범위

- 행정구역 538개(대한민국 기준행 포함)
- 관광 API 지역코드 229개
- 업체 22개와 업체 관측 105건
- 지역 지표 현재값 2,271건
- 키워드 지표 현재값 180건
- 증거파일 40개
- 업체 Snapshot 22건 중 기존 회차와 연결된 후보 10건, 검토대기 12건

원본의 결측 51건은 숫자 0으로 만들지 않고 지표행에서 제외했습니다. 실제 숫자 0은 그대로 보존합니다.

현재 로컬 작업공간에는 `tourism_data/cache` 월별 파일이 없으므로, Render 영구 디스크에 저장된 전국 행정구역 월별 자료는 아직 이 로컬 검증에 포함되지 않았습니다. 운영 전환 전에 해당 디스크를 백업한 뒤 별도 일회성 작업으로 반입·대조해야 합니다.

기존 Snapshot 후보 10건도 수집 원본의 내용 hash와 직접 연결된 영수증이 없으므로 자동으로 `현재 확정값` 포인터를 만들지 않았습니다. 수집 회차 원본과 내용 동일성을 확인한 뒤에만 승인합니다.

업체 채널 설정과 상품 단위 관측을 담을 표준 테이블은 준비되어 있습니다. 다만 현재 업체 원본에서 구조화된 상품명·채널 확인값을 복원할 수 없는 자료는 임의 생성하지 않았고, 기존 수집 회차 원본을 읽는 호환 반입 단계에서 채웁니다.

여러 서버가 동시에 DB에 쓰는 규모가 되면 같은 논리 스키마를 PostgreSQL로 이전합니다. 현재 단일 Render 서비스에서는 먼저 SQLite Shadow DB로 이관 정확성과 화면 호환성을 검증합니다.
